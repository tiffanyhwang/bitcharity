<div id ="" class="container-fluid">
  <center>
    <div class="row">
      <div class="jumbotron">
        <h1>A little bit becomes a lot</h1>
        <p>Start contributing and pick the charity you want to contribute to. At the end of the month, we'll pool together donations made by kind people like you and handle getting the funds directly to their respective charities. Proof of donations with donor names will be posted monthly.</p>
        <p><%= @button.embed_html.html_safe %></p>
      </div>
    </div>
  </center>

  <div class="panel panel-default">
    <div class="panel-heading">
    </div>
    <div class="panel-body">
      <div class="row">
        <% @charities.each do |c| %>
          <div class="col-sm-5 col-md-3">
            <div class="thumbnail">
              <%= image_tag c.logo %>
              <div class="caption">
                <h3><%= c.name %></h3>
                <p><%= c.description %></p>
                <center>
                  <p><%= button_to "Pick #{c.name}", dashboard_path({ :charity => c.id }), :class => "btn btn-primary" %><br />
                  <a href="<%= c.url %>" class="btn btn-default" role="button">Learn More</a></p>
                </center>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
